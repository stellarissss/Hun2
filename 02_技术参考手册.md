# 《混在日本》PC 单机版 — 技术参考手册

> 本手册为**开发时的速查参考**，收录所有逆向得出的技术细节。
> 与 `01_可行性与迁移方案.md` 配套使用。

---

## 目录

1. [资源服务器与路径规则](#一资源服务器与路径规则)
2. [完整资源下载清单](#二完整资源下载清单)
3. [游戏数据结构（gamedata.json）](#三游戏数据结构gamedatajson)
4. [服务端接口协议](#四服务端接口协议)
5. [JS 桥接协议](#五js-桥接协议)
6. [核心函数定位索引](#六核心函数定位索引)
7. [美术资源切分规则](#七美术资源切分规则)
8. [音频资源索引](#八音频资源索引)
9. [存档机制](#九存档机制)
10. [改造检查清单](#十改造检查清单)

---

## 一、资源服务器与路径规则

### 1.1 CDN 服务器（按优先级）

| 优先级 | 地址 | 服务商 | 实测状态 |
|---|---|---|---|
| 1 | `https://hun2-cn.oss-cn-hangzhou.aliyuncs.com/static` | 阿里云 OSS 杭州 | ✅ 200 |
| 2 | `https://hun2-en.oss-accelerate.aliyuncs.com/static` | 阿里云全球加速 | ✅ 200 |
| 3 | `https://hun2cos-1300260944.file.myqcloud.com/static` | 腾讯云 COS | ✅ 200 |
| 4 | `https://hun2web-1300260944.file.myqcloud.com` | 腾讯云 COS (web) | ✅ 200 |

> **注**：源码注释写明「腾讯云的CDN，由于不稳定，所以废弃」，生产环境优先用阿里云。

### 1.2 路径拼接函数

| 函数 | 返回 | 用途 |
|---|---|---|
| `getResPath_By_CDN()` | CDN 根 + 无 `/static` | 拼 `/audio/...` |
| `getResPath_CORS_Web()` | CDN 根 + `/static` | 拼 `/json/`、`/pics/`、`/audio/` |
| `getResPath_CORS_By_Device()` | 按设备选 CDN | 大图资源 |
| `getResPath_By_Device_AudioEffect(b)` | `getResPath_By_CDN() + "/audio/effect/"` | 音效 |
| `getImgSrc_dependentOnDevice(path, ext)` | `hasImgHD() ? path+"_HD."+ext+"?v="+imgVersion : path+"."+ext+"?v="+imgVersion` | **图集路径（关键）** |

### 1.3 资源路径模板

```
{CDN}/static/
  ├─ js/
  │   ├─ hun_min.js
  │   ├─ hun_japan/word/{cn,cn_fan,en}.js
  │   └─ lib/{jquery.min,crypto-js,LAB.min,howler.core.min,cos-js-sdk-v5.min,pako.min}.js
  │       └─ lib/pathfinding/pathfinding-browser.min.js
  ├─ json/
  │   ├─ gamedata.json
  │   ├─ version.json
  │   ├─ audio_unity.json
  │   ├─ audio_effect_min.json
  │   ├─ daily_news_epv_usa_app_china.json
  │   └─ check_connect.json
  ├─ pics/
  │   ├─ {car,person,goods,building}[_HD].jpg
  │   ├─ japan_map.png
  │   ├─ img_city_bg1.jpg / img_stuff.jpg / head_in_city.jpg / head_in_job.jpg
  │   ├─ loading_hun_hd.png / out_imgHD.png / portal_door.jpg
  │   ├─ nuclear.jpg / crowdfunding.jpg / swinghammer.jpg / takeaway.png
  │   ├─ back_to_home.jpg / fly_to_new_earth.jpg / retire_oversea.jpg / appstore.jpg
  │   └─ property/{treehouse,containerhome,cave,basement_suite,venus_city,warp_speed_ship,Kepler_452b}.jpg
  └─ audio/
      ├─ main_bgm.mp3 / func_blank.mp3
      ├─ bgm/city/{bgm_city_common,bgm_city_2,bgm_city_3,bgm_city_6,bgm_city_g_part1}.mp3
      ├─ bgm/job/{bgm_job_1,3,4,5,6,8}.mp3
      ├─ bgm/job/fight/{bgm_fight_1,bgm_fight_2}.mp3
      ├─ bgm/job/race/{bgm_race_1,bgm_race_common}.mp3
      └─ effect/{508 个音效 id}.mp3
```

---

## 二、完整资源下载清单

### 2.1 必下（核心）

```
# 逻辑
js/hun_min.js                              2,136,759
js/hun_japan/word/cn.js                      262,599
js/hun_japan/word/cn_fan.js                  262,635
js/hun_japan/word/en.js                      264,321

# 依赖
js/lib/jquery.min.js                          84,380
js/lib/crypto-js.js                          191,936
js/lib/pako.min.js                            46,993
js/lib/howler.core.min.js                      25,388
js/lib/LAB.min.js                              5,490
js/lib/pathfinding/pathfinding-browser.min.js 17,977

# 数据
json/gamedata.json                           424,243
json/version.json                              1,421
json/audio_unity.json                         33,835
json/check_connect.json                           21

# 图集（含高清）
pics/car_HD.jpg                            1,833,137
pics/person_HD.jpg                         1,772,375
pics/goods_HD.jpg                          1,079,541
pics/building.jpg                            286,563

# 主视觉
pics/japan_map.png                           223,100
pics/img_city_bg1.jpg                        409,079
pics/img_stuff.jpg                           138,493
pics/loading_hun_hd.png                       60,601
pics/portal_door.jpg                          72,725
pics/nuclear.jpg                              93,735
pics/out_imgHD.png                           288,493
pics/back_to_home.jpg                        121,635
pics/fly_to_new_earth.jpg                    115,458
pics/retire_oversea.jpg                      115,458
pics/head_in_city.jpg                          8,488
pics/head_in_job.jpg                           8,488
pics/swinghammer.jpg                           9,775
pics/takeaway.png                             13,172
pics/crowdfunding.jpg                        393,224
pics/appstore.jpg                             15,873

# 房产（日本版保留前 4）
pics/property/treehouse.jpg                   94,030
pics/property/containerhome.jpg               68,285
pics/property/cave.jpg                        73,692
pics/property/basement_suite.jpg              60,474

# BGM
audio/main_bgm.mp3                           128,567
audio/func_blank.mp3                          26,646
audio/bgm/city/bgm_city_common.mp3           499,074
audio/bgm/city/bgm_city_2.mp3              1,799,136
audio/bgm/city/bgm_city_3.mp3                    —
audio/bgm/city/bgm_city_6.mp3                    —
audio/bgm/city/bgm_city_g_part1.mp3              —
audio/bgm/job/bgm_job_1.mp3                  623,581
audio/bgm/job/bgm_job_3.mp3                      —
audio/bgm/job/bgm_job_4.mp3                      —
audio/bgm/job/bgm_job_5.mp3                      —
audio/bgm/job/bgm_job_6.mp3                      —
audio/bgm/job/bgm_job_8.mp3                      —
audio/bgm/job/fight/bgm_fight_1.mp3          448,683
audio/bgm/job/fight/bgm_fight_2.mp3              —
audio/bgm/job/race/bgm_race_1.mp3            709,516
audio/bgm/job/race/bgm_race_common.mp3           —
```

### 2.2 音效清单获取方式

```javascript
// 从 audio_unity.json 提取全部 508 个 id
const data = require('./json/audio_unity.json');
const ids = new Set();
data.effect.forEach(g => g.ids.forEach(i => ids.add(i)));
// → 508 个，路径：audio/effect/{id}.mp3
```

### 2.3 排除清单（"仅主游戏"要求）

| 排除项 | 匹配规则 |
|---|---|
| 美国版 | `*_usa*` / `city_usa` / `price_dollar` / `main_bgm_usa.mp3` / `bgm_city_usa*` |
| 混一 | `bg_hun1*` / `hun1.apk` / `hkhun1*` |
| FC 模拟器 | `emulator/fc/rom/*.nes` / `emulator_game` 数据 |
| 热血高校 | `club_area[0]`（详见 D2 决策） |
| 分享/众筹 | `share_nes/` / `crowdfunding`（可选保留） |

---

## 三、游戏数据结构（gamedata.json）

顶层：`{"Root": {...}}`

### 3.1 全局

| 字段 | 类型 | 值/说明 |
|---|---|---|
| `mapW` / `mapH` | int | `1024` / `1024` 地图尺寸 |
| `block_uid` | list | 封禁用户 id 列表（离线版忽略） |
| `game_tester` | list | 测试账号（离线版忽略） |

### 3.2 city（9 座日本城市）

```json
{
  "id": 1, "map": 1,
  "name": ["京都", "京都", "Kyoto"],       // [简中, 繁中, 英文]
  "cityPrice": 3000,
  "px": 0.45, "py": 0.67,                  // 地图相对坐标（0~1）
  "w": 80, "h": 50, "scale": 0.6,          // 显示尺寸
  "hotspot_seq": 1
}
```

**全部 9 城**：

| id | 城市 | 价格 | px | py |
|---|---|---|---|---|
| 1 | 京都 | 3000 | 0.45 | 0.67 |
| 2 | 東京 | 4000 | 0.69 | 0.62 |
| 3 | 大阪 | 2000 | 0.40 | 0.71 |
| 4 | 横浜 | 1000 | 0.64 | 0.68 |
| 5 | 名古屋 | 1000 | 0.564 | 0.642 |
| 6 | 札幌 | 1000 | 0.705 | 0.165 |
| 7 | 福岡 | 1000 | 0.125 | 0.76 |
| 9 | 仙台 | 1000 | 0.71 | 0.46 |
| 10 | 広島 | 1000 | 0.25 | 0.73 |

> **注**：id 跳过 8，说明原设计有 10 城，武汉/长沙之类可能被移除或为美国版预留。

### 3.3 car（66 辆车）

```json
{
  "id": 1,
  "name": ["斯巴鲁BRZ", "斯巴魯BRZ", "Subaru BRZ"],
  "memo": ["动力上继续搭载2.0L...", "動力上...", "This Subaru..."],
  "price": "204000~220000",         // 日元价格区间（字符串）
  "price_dollar": "27000~29000",
  "wave_min": 0.8, "wave_max": 1.2, // 价格波动
  "speed": ..., "capacity": ...,     // 速度 / 核载
  "safety": ...,                    // 安全
  "cX": ..., "cY": ...,             // 图集坐标 ★
  "role": ...
}
```

**config**：

| 字段 | 值 | 说明 |
|---|---|---|
| `pic_clip_start_x` | 0 | 图集起始 X |
| `pic_clip_start_y` | 0 | 图集起始 Y |
| `pic_clip_WH` | **200** | 单元格边长 ★ |
| `PR_nitroGlass` | 50 | 氮气玻璃参数 |
| `PR_topSpeed` | 10 | 极速参数 |
| `PR_common_host` | 40 | 通用宿主参数 |

**车辆样例**（部分）：斯巴鲁BRZ / 雷克萨斯GS / 日产Cedric(警用) / 丰田皇冠(警用) / 斯巴鲁力狮(警用) / 丰田Supra / 奔驰S400L / 日产西玛 / 哈弗H6 / 五菱宏光 …

### 3.4 actor（人物系统）

**结构**：

```
actor
 ├─ role [7]                    角色分类
 ├─ person
 │   ├─ common                  "人员属性"
 │   ├─ config {pic_clip_*: 200}
 │   └─ person [110]            ← 110 个人物
 ├─ name
 │   ├─ malename_partner_jp [29]
 │   ├─ malename_partner_cn [5]
 │   └─ femalename_partner [59]
 ├─ actor_type [15]
 └─ group [16]
```

**person 条目**：

```json
{"id": ..., "cX": ..., "cY": ..., "name": "...", "sex": ..., "age": ..., "role": ...}
```

**actor_type 15 种**：

| 常量 | 中文 | 特殊字段 |
|---|---|---|
| `MODEL_ACTOR_TYPE_LEVEL` | 等级样例（不读取） | — |
| `ACTOR_TYPE_COMMON_PEOPLE` | 群众角色 | `uncooperative_content` |
| `ACTOR_TYPE_PARTNER` | 合伙人角色 | `hire_success_content` |
| `ACTOR_TYPE_SECURITY` | 保安角色 | — |
| `ACTOR_TYPE_COP` | 警察角色 | — |
| `ACTOR_TYPE_HIGH_COP` | 特警角色 | — |
| `ACTOR_TYPE_RACER` | 赛车手角色 | `uncooperative_content` |
| `ACTOR_TYPE_STORE_EMPLOYEE` | 店员角色 | `uncooperative_content` |
| `ACTOR_TYPE_BANK_EMPLOYEE` | 银行职员角色 | `uncooperative_content` |
| `ACTOR_TYPE_BOSS_TERRORISTS` | 黑帮老大角色 | `attack_content` |
| `ACTOR_TYPE_TERRORISTS` | 黑帮小弟角色 | — |
| `ACTOR_TYPE_CREDITOR` | 债主 | `debt_reason` / `delay_content` / `attack_content` |
| `ACTOR_TYPE_SENTINEL` | 哨兵 | — |
| `ACTOR_TYPE_SOLDIER` | 自卫队 | — |

**等级属性（level）**：

| 属性 | 范围 | 说明 |
|---|---|---|
| `weapon_cool` | 10~80 | 冷兵器能力 |
| `weapon_hot` | 30~50 | 热兵器能力 |
| `charactor` | 0~100 | 角色属性 |
| `attack` | 0~100 | 攻击 |
| `leadership` | 0~100 | 领导力 |
| `goods` | 0~100 | 物品 |
| `profession_skill` | 50~80 | 职业技能 |
| `other_skill` | 50~80 | 其他技能 |
| `has_equip` | 50~80 | 装备情况 |
| `salary` | 50% | 薪资 |
| `driver_skill` | 50~80 | 驾驶技能 |
| `car_level` | 0~4 | 车辆等级 |
| `debt` | 50000~1000000 | 债务 |
| `dead_line` | 15~30 | 期限 |

**group 16 种团队**：

| 常量 | 中文 | 数量(min~) |
|---|---|---|
| `MODEL_GROUP_TYPE` | 模板 | 2 |
| `GROUP_TYPE_COMMON_PEOPLE_STREET` | 街头群众团队（街头被打劫） | 0 |
| `GROUP_TYPE_ROB_CAR` | 群众团队（被抢劫车） | 1 |
| `GROUP_TYPE_COMMON_PEOPLE_CAR` | 群众团队（被偷车时） | 1 |
| `GROUP_TYPE_COMMON_PEOPLE_HOUSE` | 群众团队（被偷盗房屋） | 2 |
| `GROUP_TYPE_SECURITY` | 保安团队 | 2 |
| `GROUP_TYPE_COP` | 警察团队 | 2 |
| `GROUP_TYPE_HIGH_COP` | 特警团队 | 2 |
| `GROUP_TYPE_RACER` | 赛车手团队 | 2 |
| `GROUP_TYPE_STORE_EMPLOYEE` | 商超职员 | 2 |
| `GROUP_TYPE_BANK_EMPLOYEE` | 银行职员 | 2 |
| `GROUP_TYPE_TERRORISTS` | 黑帮团队 | 2 |
| `GROUP_TYPE_CREDITOR` | 债主团队 | 2 |
| `GROUP_TYPE_SENTINEL` | 哨兵团队 | 1 |
| `GROUP_TYPE_SOLDIER` | 自卫队 | 2 |

**团队字段**：`actor_names` / `min_number` / `numbers` / `car_percent` / `escape`（逃逸率）/ `surrender`（投降率）/ `alert_numbers` / `alert_car_percent` / `chase_numbers` / `chase_car_percent`

### 3.5 goods（物品系统）

```
goods
 ├─ role [7] / role_scar [13] / role_steal_house [2] / role_electr [0] / role_steal_person [2] / other [5]
 ├─ config {pic_clip_WH: 200}
 ├─ goods_model [25]     ← 25 个物品模型
 └─ goods [75]           ← 75 个具体物品
```

**goods_model 字段**：

| 字段 | 说明 |
|---|---|
| `id` / `name` / `memo` | 基础 |
| `rangePrice` / `rangePrice_dollar` | 价格区间 |
| `wave_min` / `wave_max` | 波动 |
| `cX` / `cY` | 图集坐标 ★ |
| `in_store` / `in_house` / `in_car` / `in_man` | 可存放位置 |
| `role` | 分类 |
| `equip_type` | `lock,electr,shoot,acting,hide,driving` |
| `equip_postion` | `fight,body,pocket` |
| `equip_value` | 装备值 |
| `weapon_cool` / `weapon_hot` / `weapon_protective` | 武器属性 |
| `hide_value` / `scar_value` / `shouse_value` | 隐匿/撬锁/破门值 |
| `nitroglass_speed` | 切割速度 |
| `use_sound` | 使用音效 |

**goods 条目额外字段**：`hit_rate`（命中率）

### 3.6 building（14 类建筑）

```json
{"id": 1, "cX": 1, "cY": 1, "name": ["民房","民房","Regular House"], "type": "BUILDING_TYPE_HOUSE"}
```

**类型枚举**：`BUILDING_TYPE_HOUSE` / `BUILDING_TYPE_LUXURY_HOUSE` / `BUILDING_TYPE_BANK` / …

7=银行, 8=银行, 4~6=豪宅/宅邸/别墅, 1~3=民房/民屋/民宅

### 3.7 trade_goods（24 种贸易货物）

```json
{
  "name": ["顶级和牛","頂級和牛","Top Wagyu"],
  "id": 1,
  "price": 300000, "price_dollar": 30000,
  "goods_wave": [{"multiple": 1.08, "weights": 3}, ...],   // 价格波动模型
  "goods_big_wave": [...]
}
```

**货物样例**：顶级和牛 / 游戏序列号 / 少女米田共 / 飞机杯 / 充气娃娃 / 顶级手办 / 汉方药 / 进口辣条 …

**全局波动**：`minWave: 0.8` / `maxWave: 1.2`

### 3.8 turnbased（33 个兵种）

```json
{
  "id": ..., "classType": ...,
  "hp": ..., "attackValue": ..., "defenseValue": ...,
  "damageRangeValue": ..., "speed": ..., "force_value": ...,
  "upgradeTime": ..., "upgradeBlackGold": ...,
  "blackGold": ..., "unlock_time": ..., "unlock_blackgold": ..., "unlock_huncoin": ...,
  "loss_percent": ..., "loss_percent_attack": ..., "loss_percent_defense": ...,
  "name": ..., "memo": ...,
  "attack_melee_sound": ..., "movingSound": ...
}
```

> **关键**：`unlock_huncoin` 是**氪金解锁字段**，`upgradeBlackGold` 是黑金升级成本。

### 3.9 property（7 类房产）

| id | pic | 名称 | classType |
|---|---|---|---|
| 0 | treehouse.jpg | 乡村树屋 | PropertyCommon |
| 1 | containerhome.jpg | 集装箱屋 | PropertyCommon |
| 2 | cave.jpg | 洞穴 | PropertyCommon |
| 3 | basement_suite.jpg | 地下室 | PropertyCommon |
| 80 | venus_city.jpg | 火星维纳斯城 | PropertyCommon |
| 81 | warp_speed_ship.jpg | 曲速飞船 | PropertyWarpSpeedShip |
| 99 | Kepler_452b.jpg | 开普勒-452b | Property_Kepler_452b |

> **日本版仅需前 4 个**（后 3 个为美国版终局内容）。

**其他字段**：`initPriceRange` / `enableDay` / `valueWaveDays` / `valueWaveRange` / `valueWaveRange_Bought` / `valueWaveRange_EndDay` / `showIndex` / `video` / `enable`

### 3.10 club_area / club_total_rank

**club_area [3]**：`id` / `name` / `rank_title` / `black_gold_land_times` / `actors` / `traingCampLevels` / `init_land_count` / `max_actor_num` / `unLockActorIds` / `level` / `enable` / `search_land_blackgold` / `sort`

> `club_area[0].name = "热血高校"` ← **D2 决策相关**

**club_total_rank**：`id: -1` / `name` / `rank_title` / `enable: "Y"`

### 3.11 emulator_game / club_geisha

| 表 | 条目 | 字段 |
|---|---|---|
| `emulator_game` | **842** | `id` / `name_cn` / `pSF`(jpg) / `cid` |
| `club_geisha` | **800** | `id` / `jname`（日文名）/ `cname`（中文名）/ `age` / `height` |

> 按"仅主游戏"要求，`emulator_game`（游戏厅）应**排除**。
> `club_geisha` 属社团模块，随 D2 决策处理。

### 3.12 HotspotSeq（地标序列）

```json
{"seq": [15,22,17,26,8,11,16,21,13,29,27,33,10,9,20,31,6,3,14,19,28,23,18,4,34,24,30,35,2,5,36,1,12,25,32,7]}
```

36 个热点的排列顺序，3 组（对应 3 个 `club_area`？）。

---

## 四、服务端接口协议

### 4.1 接口全表（17 个）

| # | 路径 | 方法 | 参数 | 返回 | 离线实现 |
|---|---|---|---|---|---|
| 1 | `/h2/get_exparam_by_openid.do` | GET | `openid` | `{status, block, ex_params, ...}` | 返回 `{status:'OK', block:'N'}` |
| 2 | `/h2/saveProgress.do` | POST | `openid, progress...` | `{status}` | 写 localStorage |
| 3 | `/h2/gameOver.do` | POST | `c, openid, channel, nickname, dao_value, cash, debt, health, day, finish, finish_type` | `{status, rank...}` | 写本地记录 |
| 4 | `/h2/rankStuff.do` | POST | `openid` | 排行列表 | 幽灵排行 |
| 5 | `/h2/getEvent.do` | GET | `openid` | 活动列表 | 空数组 |
| 6 | `/h2/forMoney.do` | POST | 充值参数 | `{status}` | **自有渠道** |
| 7 | `/h2/exePayData.do` | POST | 支付参数 | `{status}` | **自有渠道** |
| 8 | `/h2/getHunCoinReward.do` | POST | `openid` | `{coin}` | 本地发放 |
| 9 | `/h2/consume_huncoin_record.do` | POST | `openid, record` | `{status}` | 本地流水 |
| 10 | `/h2/crowdfund.do` | POST | — | `{status}` | 本地/移除 |
| 11 | `/h2/crowdfundStuff.do` | POST | — | `{status}` | 本地/移除 |
| 12 | `/h2/checkSensitiveKeyWords.do` | POST | `word` | `{status, has}` | 本地词表 |
| 13 | `/h2club/getClubByInfo.do` | POST | `openid` | 社团信息 | 本地社团 |
| 14 | `/h2club/createClubOnly.do` | POST | `openid, name` | `{status}` | 本地社团 |
| 15 | `/h2club/getClubArea.do` | POST | `openid` | 区域列表 | 本地社团 |
| 16 | `/h2club/createClubArea.do` | POST | — | `{status}` | 本地社团 |
| 17 | `/h2club/getClubAreaByInfo.do` | POST | `openid, areaId` | 区域详情 | 本地社团 |

### 4.2 辅助接口

| 接口 | 用途 | 离线处理 |
|---|---|---|
| `/json/check_connect.json` | 连通性检测 | 本地文件 `{"status":"OK"}` |
| `https://ipapi.co/json/` | IP 归属地 | 移除/mock |
| `https://hun1.hun333.com/stat/add_point.do?point_type=` | 埋点 | **移除** |
| `http://badguys.sinaapp.com/web/jump_to_pay_qq_qun.html` | QQ 群跳转 | **移除** |
| `domainRoot + "/loginqq.action?upid="` | QQ 登录 | **移除** |

### 4.3 domainRoot 与 openid

```javascript
// domainRoot 由语言/地区决定
ex_params_japan.url = "https://h2jp.hun333.com/h2/get_exparam_by_openid.do"
ex_params_usa.url   = "https://h2jpen.hun333.com/h2/get_exparam_by_openid.do"

// 开发环境
hasGameEnv_Dev() → url = "/h2/get_exparam_by_openid.do"
```

**openid 逻辑**：

```javascript
var openid = getPageParamValue("openid");
var rew_openid = getPageParamValue("rew_openid");   // 'y' 则重写
if (rew_openid == 'y' && openid != null) localStorage.setItem("openid", openid);
var localOpenId = localStorage.getItem("openid");
if (localOpenId) openid = localOpenId;
else localStorage.setItem("openid", openid);
```

**离线版**：生成固定本地 ID `local_player_{timestamp}`，写入 localStorage。

### 4.4 c 参数（缓存击穿）

所有 POST 都带 `c` 参数：
```javascript
c: (new Date).getMilliseconds() + "_" + (new Date).getMinutes()
```
离线版可忽略，但**为兼容请原样保留**。

---

## 五、JS 桥接协议

### 5.1 JS → 原生（`jsbridge_android.callbackJs`）

**协议**：`"命令&key1=val1&key2=val2"`（`&` 分隔）

| 命令 | 参数 | 返回值 | PC 实现 |
|---|---|---|---|
| `outlink` | `url` | — | `shell.openExternal(url)` |
| `googleplay` | `pid` | — | **转自有渠道** |
| `googleplayReview` | — | — | 打开商店页（可禁用） |
| `saveKY` | `k`, `v` | — | 本地 KV |
| `getKY` | `k` | `value` | 本地 KV |
| `deleteKY` | `k` | — | 本地 KV |
| `audiovibrate` | — | — | 震动（PC 可忽略） |
| `base_url` | — | `baseURL` | 返回 `''` |
| `reset_label_text` | — | — | no-op |
| `remove_splash` | — | — | no-op |
| `show_splash` | — | — | no-op |
| `remove_reloadbutton` | — | — | no-op |
| `uuid` | — | 设备 ID | 生成并持久化 |
| `getSystemMemory` | — | 字节 | 返回固定值 |
| `getAppMaxMemory` | — | 字节 | 返回固定值 |
| `getAppTotalMemory` | — | 字节 | 返回固定值 |
| `getAppFreeMemory` | — | 字节 | 返回固定值 |
| `checkRoot` | — | `"true"/"false"` | **返回 `"false"`** |

**Android 侧静态字段**（需在 PC 侧对齐）：

```java
public static String lastHunCoin  = "0";
public static String lastPayMoney = "0";
public static String out_trade_no = "";
```

### 5.2 原生 → JS（`evaluateJavascript`）

**PC 侧必须实现这些全局函数**（否则游戏报错）：

| JS 函数 | 触发时机 | 参数 |
|---|---|---|
| `fromAndroid_GooglePlayResult(...)` | Google 支付结果 | 支付结果 |
| `fromAndroid_WxPayResult(...)` | 微信支付结果 | 支付结果 |
| `fromAndroid_AlipayResult(...)` | 支付宝结果 | 支付结果 |
| `fromAndroid_ShareQQResult(...)` | QQ 分享结果 | 分享结果 |
| `fromAppOpenShowLoading()` | 打开 Loading | — |
| `fromAppCloseShowLoading()` | 关闭 Loading | — |
| `fromAppOpenAccessibility()` | 打开无障碍 | — |

> `fromAppOpenAccessibility` 在 PC 版可空实现。

### 5.3 Android 侧完整命令解析（源码参考）

```java
@JavascriptInterface
public String callbackJs(String str) {
    // str 形如 "outlink&url=https://..."
    String[] parts = str.split("&");
    String cmd = parts[0];
    // ... 解析 key=value
}
```

---

## 六、核心函数定位索引

> 在 `hun_min.js` 中检索这些字符串可快速定位改造点。

### 6.1 资源路径（必须改造）

| 检索字符串 | 作用 | 改造 |
|---|---|---|
| `getResPath_CORS_Web` | CDN + `/static` | 返回 `''` |
| `getResPath_By_CDN` | CDN 根 | 返回 `''` |
| `getResPath_CORS_By_Device` | 按设备选 CDN | 返回 `''` |
| `getImgSrc_dependentOnDevice` | **图集路径** | 返回本地路径（保留 `_HD` 逻辑） |
| `domainRoot` | API 根 | 由 AJAX 拦截处理 |

### 6.2 网络调用（必须拦截）

| 检索字符串 | 次数 | 说明 |
|---|---|---|
| `$.ajax` | 多 | jQuery AJAX（拦截点） |
| `check_connect` | 1 | 连通性检测 |
| `add_point` | 1 | 埋点 |
| `ipapi.co` | 1 | IP 归属地 |

### 6.3 桥接（必须替换）

| 检索字符串 | 说明 |
|---|---|
| `jsbridge_android` | 桥接对象名（**16 处引用**） |
| `fromAndroid` | 回调函数（**14 处**） |
| `hasCheckNetWorkConnect` | 网络检测状态 |
| `invokeOnline` | 联网回调 |

### 6.4 版本与语言

| 检索字符串 | 说明 |
|---|---|
| `hasVersionUSA` | **美国版判断 → 强制 false** |
| `hasHeadPic_USA` | 美国头像 → false |
| `LANGUAGE_ZH` / `LANGUAGE_ZH_FAN` / `LANGUAGE_EN` / `LANGUAGE_JA` | 语言枚举 |
| `getLanguageJS` | 语言脚本路径 |
| `KEY_CACHE_LANGUAGE` | 语言缓存键 |
| `imgVersion` | 图片版本（固定 219） |
| `gameVersion` / `FRONT_VERSION` | 版本号（固定 39396） |

### 6.5 存档

| 检索字符串 | 说明 |
|---|---|
| `progressManager` | **50 处**，进度管理 |
| `saveProgress` | 存档方法 |
| `loadData` | 读档（99 处） |
| `localStorage.setItem` | 本地存储 |
| `AE_encrypt` / `AE_decrypt` | **47 处**，本地加解密 |
| `getDataForCache` | 缓存参数生成 |

### 6.6 系统管理器

| 类/对象 | 说明 |
|---|---|
| `mainManager` | 主管理器 |
| `progressManager` | 进度 |
| `playManager` | 玩法 |
| `cityManager` | 城市 |
| `boardUserInfoInMap` | 地图用户信息 |
| `loadResManager` | 资源加载 |
| `gameData` | 数据（= gamedata.json） |
| `playerGroup` | **玩家团队（375 处，核心）** |
| `actorManager` / `carManager` / `buildingManager` | 各子系统 |
| `propertyManager` | 房产 |
| `moduleFunctionManager` | 模块开关 |
| `imgCrowdfunding` | 众筹图 |

### 6.7 高清判断（PC 端自动生效）

```javascript
this.hasImgHD = function () {
  return hasGameEnv_Dev() ? !0
    : channel == CHANNEL_IOS
      ? ("null" != deviceTotalMemory && "null" != devicePlatForm
          ? (3263815936 > parseInt(deviceTotalMemory) ? !1 : !0)
          : !1)
      : !0;      // ★ 非 iOS → 恒 true
};
```

> **PC 端（非 iOS）自动返回 true → 使用 `_HD` 高清图集。**

---

## 七、美术资源切分规则

### 7.1 图集规格

| 图集 | 尺寸 | 网格 | 单元格 | 条目数 |
|---|---|---|---|---|
| `car(_HD).jpg` | 2000×2000 | 10×10 | 200×200 | 66 |
| `person(_HD).jpg` | 2000×2200 | 10×11 | 200×200 | 110 |
| `goods(_HD).jpg` | 2000×2000 | 10×10 | 200×200 | 75 |
| `building.jpg` | 2000×400 | 10×2 | 200×200 | 14 |

### 7.2 切分公式

```javascript
// gamedata.json 中 config
const { pic_clip_start_x: ox, pic_clip_start_y: oy, pic_clip_WH: W } = config;

// 每个条目的 cX / cY
const sx = ox + cX * W;
const sy = oy + cY * W;

// Canvas 绘制
ctx.drawImage(atlasImage, sx, sy, W, W, destX, destY, destW, destH);

// 若用 CSS background-position
element.style.backgroundPosition = `-${sx}px -${sy}px`;
element.style.width = W + 'px';
element.style.height = W + 'px';
```

### 7.3 主地图坐标

```javascript
// japan_map.png 为 850×1000
// 城市坐标 px/py 为相对比例
const cityScreenX = px * mapDisplayWidth;
const cityScreenY = py * mapDisplayHeight;
```

### 7.4 高清切换

```javascript
// 原逻辑
const src = hasImgHD()
  ? `${base}/pics/${name}_HD.${ext}?v=${imgVersion}`
  : `${base}/pics/${name}.${ext}?v=${imgVersion}`;
```

**离线版**：保留 `hasImgHD()` 返回 true，但去掉 `?v=` 查询串（或本地保留）。

---

## 八、音频资源索引

### 8.1 BGM 触发场景

| 文件 | 场景 |
|---|---|
| `main_bgm.mp3` | 主界面 |
| `bgm_city_common.mp3` | 城市（通用） |
| `bgm_city_2/3/6.mp3` | 特定城市 |
| `bgm_city_g_part1.mp3` | 城市（分段） |
| `bgm_job_1/3/4/5/6/8.mp3` | 各类任务 |
| `bgm_fight_1/2.mp3` | 战斗 |
| `bgm_race_1/common.mp3` | 赛车 |

### 8.2 音效分组（188 组 / 508 个）

| 分组 desc | 音效 ids | 数量 |
|---|---|---|
| 棍子挥舞的声音 | `swing_1` | 1 |
| 子弹击中身体 | `bullet_hit_body_1~5` | 5 |
| 飞机场声音 | `airport_1~2` | 2 |
| 飞机起飞声音 | `plane_fly_1~3` | 3 |
| 艺伎被解救声音 | `robed_geicha_1~4` | 4 |
| …（共 188 组） | | 508 |

**命名规律**（从 ids 观察）：

| 前缀 | 含义 |
|---|---|
| `ak47_` / `ar50_` / `awp_` | 枪械射击 |
| `afraid_female_` / `afraid_male_` | 惊慌 |
| `angry_man_` | 愤怒 |
| `arrest` | 逮捕 |
| `atm` | ATM |
| `bullet_hit_body_` | 中弹 |

### 8.3 音频引擎

游戏使用 **howler.js**（`js/lib/howler.core.min.js`），离线化无需改造，仅需路径正确。

---

## 九、存档机制

### 9.1 存储位置

| 键 | 用途 |
|---|---|
| `localStorage["openid"]` | 玩家标识 |
| `localStorage["game_language"]` | 语言选择 |
| 其他（由 `LocalCacheImpl_LocalStorage` 管理） | 进度与资产 |

### 9.2 缓存实现

```javascript
function LocalCacheImpl_LocalStorage() {
  this.setCache = function (key, val) { localStorage.setItem(key, val); };
  this.getCache = function (key) { return localStorage.getItem(key); };
  // ...
}
```

### 9.3 加密

| 函数 | 实现 | 用途 |
|---|---|---|
| `AE_encrypt` | crypto-js | 存档字段加密 |
| `AE_decrypt` | crypto-js | 解密 |
| `AE_encrypt_Int` | 整数加密 | 数值保护 |

**关键**：密钥**内置在 hun_min.js 中**，不需要服务端。这证明存档是自包含的。

### 9.4 防强退机制（需保留）

| 机制 | 文案 |
|---|---|
| 任务中退出扣技能 | 「任务中退出游戏，将会扣除团队成员各技能值 10~15 点」 |
| 未上传结果视为强退 | 「未上传任务结果将视为强制退出，会扣除角色相应的技能值」 |
| 申请救援扣技能 | `ARR_REDEEM_CAR_NOT_ENOUGH_SITS` |

> 离线版需保留这些逻辑（在本地 `saveProgress` 中实现）。

### 9.5 离线版增强

```javascript
// 建议：存档 HMAC 签名
const SAVE_KEY = 'hjo_save_v1';
const SIG_KEY  = 'hjo_sig_v1';   // 内置密钥

function save(data) {
  const payload = JSON.stringify(data);
  const sig = CryptoJS.HmacSHA256(payload, EMBEDDED_KEY).toString();
  localStorage.setItem(SAVE_KEY, payload);
  localStorage.setItem(SIG_KEY, sig);
}

function load() {
  const payload = localStorage.getItem(SAVE_KEY);
  const sig = localStorage.getItem(SIG_KEY);
  if (CryptoJS.HmacSHA256(payload, EMBEDDED_KEY).toString() !== sig) {
    throw new Error('存档已损坏或被篡改');
  }
  return JSON.parse(payload);
}
```

---

## 十、改造检查清单

### 10.1 资源层

- [ ] 下载全部 9 个 JS（含依赖库）
- [ ] 下载 3 套文案（cn / cn_fan / en）
- [ ] 下载 gamedata.json 并**精简移除 USA**
- [ ] 下载 4 张图集（高清版）
- [ ] 下载全部场景图（约 20 张）
- [ ] 下载 12 首 BGM
- [ ] 下载 **508 个音效**（从 audio_unity.json 提取）
- [ ] 生成 `manifest.json` 校验文件
- [ ] 排除 `*_usa*` / `bg_hun1*` / FC ROM

### 10.2 代码层

- [ ] 创建 `offline-adapter.js`（AJAX 拦截）
- [ ] 创建 `native-bridge.js`（桥接替换）
- [ ] 创建 `hun-jp-callbacks.js`（回调补齐）
- [ ] 改造 `index.html` 脚本引入顺序
- [ ] `getResPath_*` 系列 → 本地路径
- [ ] `hasVersionUSA()` → 强制 false
- [ ] 屏蔽 `add_point` / `ipapi.co` 埋点
- [ ] `version.json` 固定 FRONT_VERSION=39396
- [ ] `imgVersion` 固定 219

### 10.3 接口层（17 个）

- [ ] `get_exparam_by_openid.do` → 本地配置
- [ ] `saveProgress.do` → localStorage
- [ ] `gameOver.do` → 本地记录
- [ ] `rankStuff.do` → 幽灵排行
- [ ] `getEvent.do` → 空活动
- [ ] `getHunCoinReward.do` → 本地发放
- [ ] `consume_huncoin_record.do` → 本地流水
- [ ] `checkSensitiveKeyWords.do` → 本地词表
- [ ] `club/*`（5 个）→ AI 社团
- [ ] `crowdfund*.do`（2 个）→ 本地/移除
- [ ] `forMoney.do` / `exePayData.do` → **自有渠道**

### 10.4 支付层

- [ ] 对接自有渠道 SDK
- [ ] SKU 映射（199/299/399/499）
- [ ] 支付回调 → `fromAndroid_*Result`
- [ ] 魂币发放（凭渠道回调）
- [ ] 存档签名防篡改
- [ ] 订单对账（可选联网）

### 10.5 打包层

- [ ] Electron 主进程
- [ ] 本地 HTTP 服务
- [ ] 竖屏窗口配置
- [ ] electron-builder 配置
- [ ] 跨平台构建

### 10.6 测试层

- [ ] F1 断网启动
- [ ] F2 断网全流程
- [ ] F3 存档持久化
- [ ] F4 9 城切换
- [ ] F5 战斗系统
- [ ] F6 技能系统
- [ ] F7 飙车系统
- [ ] F8 贸易系统
- [ ] F9 小游戏
- [ ] F10 结局
- [ ] F11 内购链路
- [ ] F12 多语言
- [ ] V1~V6 外观验收
- [ ] T1~T5 技术验收

---

**手册版本**：v1.0 · 2026-09-19
